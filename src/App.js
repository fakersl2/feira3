import { Rotas } from './Rotas'; // Importa o componente Rotas do arquivo Rotas.js
import './App.css'; // Importa o arquivo de estilo CSS App.css

// Componente funcional App
const App = () => {
  return (
    <Rotas /> // Renderiza o componente Rotas, que gerencia a navegação da aplicação
  );
};

export default App; // Exporta o componente App como padrão
